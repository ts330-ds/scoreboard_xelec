import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Backgrounds
  static const Color bg       = Color(0xFF0B0D12);
  static const Color surface  = Color(0xFF12151C);
  static const Color surface2 = Color(0xFF181C26);
  static const Color border   = Color(0xFF222736);

  // Accents
  static const Color cyan     = Color(0xFF00D4FF);
  static const Color red      = Color(0xFFFF4560);
  static const Color green    = Color(0xFF00E676);
  static const Color yellow   = Color(0xFFFFC107);
  static const Color purple   = Color(0xFFA78BFA);
  static const Color orange   = Color(0xFFFF8C42);

  // Text
  static const Color textPrimary = Color(0xFFDDE4F0);
  static const Color textMuted   = Color(0xFF5A6680);
  static const Color textMuted2  = Color(0xFF8A95AA);

  // Semantic
  static const Color success  = green;
  static const Color warning  = yellow;
  static const Color error    = red;
  static const Color info     = cyan;
}
