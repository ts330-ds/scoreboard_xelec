import 'package:flutter/material.dart';
class Step2AthletesScreen extends StatelessWidget {
  const Step2AthletesScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Step2')));
}
