import 'package:flutter/material.dart';
// TODO: Full implementation coming in Phase 2 & 3
class PlaceholderScreen extends StatelessWidget {
  const PlaceholderScreen({super.key});
  @override Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Coming soon')));
}
