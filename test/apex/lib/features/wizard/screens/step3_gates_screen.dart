import 'package:flutter/material.dart';
class Step3GatesScreen extends StatelessWidget {
  const Step3GatesScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Step3')));
}
