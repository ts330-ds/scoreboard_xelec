import 'package:flutter/material.dart';
class Step4ConfigScreen extends StatelessWidget {
  const Step4ConfigScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Step4')));
}
