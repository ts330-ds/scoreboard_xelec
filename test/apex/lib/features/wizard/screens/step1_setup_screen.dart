import 'package:flutter/material.dart';
class Step1SetupScreen extends StatelessWidget {
  const Step1SetupScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Step1')));
}
