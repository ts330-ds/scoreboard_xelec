import 'package:flutter/material.dart';

class AppColors {

  AppColors._();
  // Background
  static const Color background = Color(0xFF0C1117);

  // Team Colors
  static const Color lakersGreen = Color(0xFF3D8F2F);
  static const Color celticsBlue = Color(0xFF2E7CF6);

  // Score Colors
  static const Color scoreOrange = Color(0xFFFF5A1F);

  // Center Panel
  static const Color panelBorder = Color(0xFFFF5A1F);
  static const Color shotClockRed = Color(0xFF8B0000);

  // Text
  static const Color white = Colors.white;
  static const Color mutedWhite = Colors.white70;

  // Card
  static const Color cardDark = Color(0xFF151A20);

  // Main background colors
  static const Color darkBackground = Color(0xFF161C24);
  static const Color lightBackground = Color(0xFFE8E8E8);

  // Primary (Accent) colors
  static const Color primaryBlue = Color(0xFF4285F4);

  // Text & Icon colors
  static const Color lightText = Color(0xFFE5E5E5);
  static const Color darkText = Color(0xFFFFFFFF);
  static const Color subtleText = Color(0xFF9095A1);

  // Input field colors
  // The darkBackground color with 30% opacity
  static final Color inputFillColor = darkBackground.withOpacity(0.3);
  static const Color inputBorderColor = Color(0xFF333D48);

  // Button colors
  static const Color blueButton = Color(0xFF4C7FFF); // Slightly different blue for primary button
  static const Color socialButtonBorder = Color(0xFF333D48);

  static const Color gradientTop = Color(0xFF1E2741); // Darker Blue/Navy (Top)
  static const Color gradientBottom = Color(0xFF2A3654); // Slightly Lighter Blue/Navy (Bottom)

  // Background Gradient
  static const Color bgGradientStart = Color(0xFF0B133F);
  static const Color bgGradientMiddle = Color(0xFF0D1E6D);
  static const Color bgGradientEnd = Color(0xFF0A0F2C);

  // Primary
  static const Color lightBlueText = Color(0xFF6EA8FF);

  // Text
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white70;
  static const Color textHint = Colors.white54;

  // Input
  static const Color inputBackground =
  Color.fromRGBO(255, 255, 255, 0.08);
  static const Color inputBorder =
  Color.fromRGBO(255, 255, 255, 0.24);

  // Divider
  static const Color divider = Colors.white24;

  // Icon Glow
  static const Color iconGlow = Colors.blueAccent;
}
